# V1-D10：outer 族跨轨迹扩产 + 规则 v2.2（2026-07-24）

## S3 采集与跨族评估

- 3 次单次 Isaac 运行（seed 49/50/51 × outer_lateral_patrol，各 59 帧、4 窗口：
  approach 55–135 / sweep 165–245 / retreat 345–415 / static_idle 430–475），
  exit 全 0，无重试；链路评估 177 track POST，无 GT 种子、无 VLM。
- **跟踪 12/12 窗口 100% 命中（177/177 帧）**——两个轨迹族、六个 seed、
  全相位下感知层无一失手。

## v2.1 跨族成绩：10/12，两个错误

1. **seed 49 approach 漏报**（fail-closed）：平移 17.2、缩放 7.5——慢速深度
   运动落在平移阈值（25）与深度缩放阈值（18）之间的灵敏度间隙。
2. **seed 51 static_idle 误报（F12，预注册风险首次实证）**：G1 在 retreat 后
   停在极近距离，框上下边同时贴满画幅（高度恒 479），SAM2 掩码"呼吸"使宽度
   203→175，缩放 18.7、aspect 0.148 跨过深度阈值——但平移仅 2.7 px/s，
   无真实运动。深度通道的"形状特征无法区分运动与掩码形变"风险成真。

## 发现 F12 与 v2.2 修复

- **F12**：近距特写下（框双 y 边裁剪、高度信号失效），静止目标的掩码宽度
  呼吸可伪造深度运动的缩放/aspect 签名；判别性信号是平移——真实深度运动
  必然携带离轴透视平移（7 个深度真值窗口最小 10.5 px/s），纯掩码呼吸没有
  （2.7 px/s）。
- **v2.2**：深度通道增加平移下限 8 px/s（`depth_translation_floor_px_s`），
  余量：真值侧 10.5/8 ≈ 1.3x，呼吸侧 8/2.7 ≈ 3x。同时输出
  `both_y_edges_clipped` 审计诊断（暂不作门禁）。
- 诚实性：该下限的标定包含了触发它的失败样本本身（事后标定），需在后续
  批次中前瞻性复验。
- 单测 8/8（新增呼吸拒绝 + 真深度通过两用例）。

## v2.2 全量成绩单（24 窗口 = mirrored 12 + outer 12）

**23/24，全部残余错误为 fail-closed 漏报，零误报。**
唯一漏报：outer seed 49 慢速 approach（平移 17.2 落入间隙）。

| 规则版本 | mirrored 12 | outer 12 | 合计 |
|---|---|---|---|
| v1（瞬时速度+D4A） | 4/12 | — | — |
| v2（平移通道） | 9/12 | — | — |
| v2.1（+深度通道） | 12/12 | 10/12 | 22/24 |
| **v2.2（+平移下限）** | **12/12** | **11/12** | **23/24** |

## 数据集状态

manifest 3.9.0：新增 12 个 outer 族 provisional 条目（动态正 3、深度运动正 6、
静态负 3），待人工审查（拼图 v1d10_review_packet_20260724/）。
审查通过后总量：动态正 22、静态负 7、跨 2 轨迹族 × 7 seed × 4 相位。

## 边界

- seed 49 approach 类慢速深度运动仍在灵敏度间隙内（fail-closed）。
- v2.2 平移下限为事后标定，待前瞻复验。
- 泄漏-深度混淆的根本解仍开放（候选：双 y 边裁剪门禁、深度通道）。

## 预算

Isaac 运行 3 次（无重试）、track POST 177、VLM 0。

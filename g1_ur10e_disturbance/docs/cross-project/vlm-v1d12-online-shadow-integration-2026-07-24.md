# V1-D12（B6）：在线影子集成——实时闭环验证（2026-07-24）

## 目的

此前所有规则验证均为离线回放（先采集后评估）。本轮把感知链 + 规则 v2.2
接入**与仿真并发的实时流**：宿主侧监督进程轮询 Isaac 正在写出的帧，逐帧
增量执行 SAM2 跟踪 → 滑动窗口运动评估（14 帧 ≈ 65 步，与标定窗长一致）→
规则 v2.2 判决，动作只记录不执行（影子模式）。闭合"离线验证 → 实时系统"
的最后一环。

## 协议

- 单次 Isaac 运行：mirrored patrol，seed 53（未见），max_steps 545，
  采集步 60–540（每 5 步，97 帧），横跨全部 6 个脚本相位。
- 监督器先于仿真启动，实时处理；窗口未满时 fail-closed。
- 预注册门禁（写入脚本 docstring，先于运行）：
  G1 扫掠核心窗口全触发；G2 retreat 核心 ≥5/6；G3 idle 核心零误报；
  G4 实时性（最大积压 ≤2 帧且处理完 97/97）。
  approach 核心为观察项（已知慢速间隙）。

## 结果：D12_ONLINE_SHADOW_PASS（4/4 门禁）

| 门禁 | 结果 |
|---|---|
| G1 扫掠核心（neg {245,250} + pos {315,320}） | 4/4 触发 ✓ |
| G2 retreat 核心 {385–410} | 5/6 触发 ✓ |
| G3 idle 核心 {475–540} | 0/14 误报 ✓ |
| G4 实时性 | 最大积压 0 帧，97/97 处理完 ✓ |

观察项：
- approach 核心 5/5 触发（慢速间隙未复现，深度通道生效）。
- 逐帧时延：中位 201 ms、p95 217 ms、最大 446 ms——远低于帧到达节奏
  （每 5 仿真步约 1 s 壁钟），监督器全程零积压，具备实时余量约 5x。
- 感知 97/97 帧命中（累计 617/617）。

## 结论与边界

- 五阶段架构的"感知 → 证据 → 符号规则"链路首次在实时并发条件下完成
  全相位影子验证；相位转换期窗口（跨相位）判决未设门禁，时间线全量
  落盘（timeline.jsonl）可供逐帧审计。
- 仍为影子模式：动作建议（slow_down）未回注控制。Active semantic
  control（B7）需用户决策。
- 滑动窗口引入约 65 仿真步（≈1.1 s 仿真时间）的检测延迟，是窗口聚合
  方法的固有代价，论文中应如实报告。

## 预算

Isaac 运行 1 次（无重试）、track POST 97、VLM 0。

# V1-D7A.1：D7A 边界批次人工标签审查（2026-07-24）

审查人：用户（明确批复"批准"，2026-07-24）。
审查材料：4 个采集各自窗口的首/末帧（共 8 帧，随 D7A 汇报内联呈现）。

| 候选 | 审查判据 | 结论 |
|---|---|---|
| E02-DYN-D7A-B1-REVERSE | 步 250 vs 315 可见 G1 左移（反向扫掠） | 批准为动态正样本 |
| E02-DYN-D7A-B2-RETREAT-DEPTH | 步 325 vs 400 可见 G1 变大（沿相机轴接近） | 批准为动态正样本（深度边界标记保留） |
| E02-STA-D7A-B3-IDLE | 步 420 vs 465 G1 基本静止（仅平衡微晃） | 批准为静态负样本 |
| E02-DYN-D7A-B4-OUTER | 步 165 vs 245 可见 G1 左移（Dyn-B 外侧车道） | 批准为动态正样本 |

manifest 升至 3.6.0。动态正样本组现含 E2K、b1、b2、b4；静态负样本组含 b3。
b2 的深度边界标记保留：其像素平移证据天然衰减（14.5 px/s），当前证据层
v1/v2 均 fail-closed 漏报，作为已知局限样本参与后续深度运动判别器验证。

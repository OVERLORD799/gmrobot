# V1-D11：规则 v2.2 前瞻性验证（outer seed 52，2026-07-24）

## 目的

v2.2 的深度通道平移下限（8 px/s）标定时包含了触发它的 S3 失败样本（事后
标定）。本轮用未见过的 outer seed 52 做预测先行的前瞻复验，关闭该诚实性缺口。

## 协议

单次 Isaac 运行（exit=0，无重试），4 窗口 59 帧；预注册门禁（跟踪前写入）：
sweep 触发、retreat 触发、static_idle 不触发；approach 为观察项（已知间隙）。
链路无 GT 种子，59 track POST，0 VLM。

## 结果：D11_PROSPECTIVE_PASS（门禁 3/3，判决 4/4 正确）

| 窗口 | GT | 平移 | 缩放 | aspect | 判决 | 通道 |
|---|---|---|---|---|---|---|
| approach | 动 | 19.9 | 33.4 | 0.440 | 触发 ✓ | 深度 |
| sweep | 动 | 56.0 | 14.2 | 0.043 | 触发 ✓ | 平移 |
| retreat | 动 | 17.9 | 22.3 | 0.123 | 触发 ✓ | 深度 |
| static_idle | 静 | 11.2 | 19.3 | 0.035 | 拒绝 ✓ | — |

- 观察项 approach 由深度通道正确捕获（seed 49 型间隙未复现）。
- 跟踪 59/59 帧全中；感知层累计约 28 窗口零失手。

## 重要诚实性观察（记录为 F13）

本次 static_idle 的拒绝**不是**由 v2.2 平移下限完成的：其平移 11.2 ≥ 8、
缩放 19.3 ≥ 18 均越过阈值，唯一挡住它的是 aspect 0.035 < 0.05（余量仅
1.4x）。深度通道三条件是联合承载的——微晃平移 + 掩码呼吸缩放的组合出现时，
aspect 是最后一道防线。若后续观察到 aspect ≥ 0.05 的静止掩码呼吸，深度
通道将出现误报（安全侧 slow_down）。此为当前已知最薄弱余量，跨批次持续监测。

## 规则 v2.2 累计战绩

回溯 23/24（两族 12+12，零误报）+ 前瞻 4/4（seed 52）。
seed 52 帧仅用于规则验证，未注册为数据集候选。

## 预算

Isaac 运行 1 次（无重试）、track POST 59、VLM 0。

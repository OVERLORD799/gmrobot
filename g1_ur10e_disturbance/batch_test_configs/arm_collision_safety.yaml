name: "arm_collision_safety"
description: "arm_collision scripted scenario + safety_fusion (STOP stress test)"
mode: "scripted"
scenario: "arm_collision"
safety:
  config_path: "configs/safety_fusion.yaml"
  enable_envelope: true
episode:
  max_steps: 5000
  num_parts: 20
output:
  step_csv: true
  episode_json: true
